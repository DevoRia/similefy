export enum FileType {
  TXT = 'text/plain',
  //TODO not supported yet HTML = 'text/html',
  // JSON = 'application/json',
}

export enum FileExt {
  TXT = 'txt',
  HTML = 'html',
  JSON = 'json',
}
